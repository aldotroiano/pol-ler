apiVersion: v1
kind: Secret
metadata:
  name: {{ include "pol-ler.fullname" . }}-postgres
  labels:
    {{- include "pol-ler.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
type: Opaque
stringData:
  postgres-password: {{ .Values.postgres.password | quote }}

